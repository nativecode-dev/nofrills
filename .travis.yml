cache: yarn
language: node_js
node_js:
  - node
  - '8'
  - '6'
os:
  - linux
after_success:
  - yarn run coveralls
stages:
  - test
  - name: deploy-master
    if: branch = master AND type != pull_request
jobs:
  include:
    - stage: deploy-master
      script: yarn run push
