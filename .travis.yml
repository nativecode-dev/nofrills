cache:
  directories:
    - node_modules
  yarn: true
language: node_js
node_js:
  - 'node'
os:
  - linux
  - osx
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s
  - export PATH="$HOME/.yarn/bin:$PATH"
  - yarn global add @nofrills/tasks@5.6.2
  - yarn install
  - cli-tasks configure:git
  - cli-tasks configure:ssh
stages:
  - test
  - name: coverage
    if: branch = "master" OR branch = "develop"
  - name: release
    if: branch = "master" AND type != pull_request
jobs:
  include:
    - stage: coverage
      node_js: '8'
      os: linux
      script:
        - cli-tasks coverage
    - stage: release
      node_js: '8'
      os: linux
      script:
        - git remote set-url origin git@github.com:nativecode-dev/nofrills.git
        - git checkout $TRAVIS_BRANCH
        - npm whoami
        - cli-tasks release
after_failure:
  - cat lerna-debug.log
