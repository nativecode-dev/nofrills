const expect = require('chai').expect

describe('when using types', () => {
  describe('and registering new type', () => {
    it('should replace existing type definition', () => {

    })
  })

  describe('and resolving type', () => {

  })
})
