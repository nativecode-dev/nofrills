#!/bin/bash

DOMAIN="nativecode.net"

export BUILD_REPO="nativecode/nofrills"
export BUILD_REPO_BRANCH=${CI_COMMIT_REF_NAME}
export BUILD_REPO_DOMAIN="git.${DOMAIN}"
export BUILD_REPO_DOMAIN_SSH="gitssh.${DOMAIN}"
export BUILD_REPO_EMAIL="automaton+nofrills@nativecode.com"
export BUILD_REPO_SSHKEY_PATH="/file-not-found"

#-------------------------------------------------------------------------------
# DO NOT EDIT: Sets common access urls                                         #
#-------------------------------------------------------------------------------
export BUILD_REPO_GIT="git@${BUILD_REPO_DOMAIN}:${BUILD_REPO}.git"
export BUILD_REPO_URL="https://${BUILD_REPO_DOMAIN}/${BUILD_REPO}.git"
export BUILD_REPO_URL_SSH="ssh://${BUILD_REPO_GIT}"

#-------------------------------------------------------------------------------
# DO NOT EDIT: Sets the path to include node_modules                           #
#-------------------------------------------------------------------------------
export PATH=./node_modules/.bin:${PATH}
